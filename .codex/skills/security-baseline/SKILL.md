---
name: security-baseline
description: 커밋/릴리즈 전 이 프로젝트의 최소 보안 baseline 을 점검하고 pass/fail 을 보고한다. palab-platform 보안 정책 기준. 커밋 전 또는 릴리즈 준비 시 사용.
---

# security-baseline (Codex / OpenClaw 어댑터)

이 skill 의 **정본 절차**는 이 프로젝트의 `ops/skills/security-baseline.md` 에 있다.

1. `ops/skills/security-baseline.md` 를 읽는다.
2. 그 안의 절차 1~7 을 이 repo 에 대해 수행한다.
3. 정의된 출력 형식(각 항목 pass/fail/unknown + 근거)으로 보고한다.

판단 기준·정책은 그 파일이 링크하는 palab-platform 문서를 따른다. 이 어댑터에는 규칙을 복제하지 않는다.
